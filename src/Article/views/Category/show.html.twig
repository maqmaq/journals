{% extends "Template/layout.html.twig" %}

{% block content %}

    <dl>
        <dt>Id</dt>
        <dd>{{ category.getId() }}</dd>

        <dt>Name</dt>
        <dd>{{ category.getName() }}</dd>

        <dt>Articles</dt>
        <dd>
            {% if articles|length %}
                {% include 'partial/Article/ordered-list.html.twig' %}
            {% else %}
                List is empty
            {% endif %}
        </dd>

    </dl>
{% endblock %}